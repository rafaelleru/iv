 version: 2
 jobs:
   build:
     docker:
       - image: circleci/python:3.7.1
     steps:
       - checkout
       - run: pip install --upgrade pip
       - run: pip install -r requirements.txt
       - run: pytest tests/
